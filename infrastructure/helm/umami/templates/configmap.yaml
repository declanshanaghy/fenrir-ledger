apiVersion: v1
kind: ConfigMap
metadata:
  name: umami-init-db
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "umami.labels" . | nindent 4 }}
data:
  # Umami handles schema initialization automatically via migrations
  # This ConfigMap is here for future database initialization needs
  init.sql: |
    -- PostgreSQL initialized with default values from environment
    -- Umami migrations will run automatically on first startup

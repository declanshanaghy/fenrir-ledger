{
  "buildCommand": "mkdir -p public/sessions public/static && cp -r ../../sessions/. public/sessions/ && cp -r ../../static/. public/static/ && next build",
  "ignoreCommand": "git diff HEAD^ HEAD --name-only | grep -qE '^(development/src/|static/|sessions/|vercel\\.json|\\.github/workflows/)'"
}

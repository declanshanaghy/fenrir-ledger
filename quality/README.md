# ⚔️ Quality Assurance — Fenrir Ledger

**Loki's domain.** FiremanDecko builds. Loki tears down. The shield wall holds.

---

## Test Architecture

### Three-Layer Pyramid

| Layer | Runner | Tests | Speed | Purpose |
|-------|--------|-------|-------|---------|
| **Unit** | Vitest | ~500+ | ~2s | Pure logic, utilities, helpers |
| **Integration** | Vitest + happy-dom | ~300+ | ~3s | API routes, component render, hooks |
| **E2E** | Playwright | ~159 | ~6min | User journeys, critical integrations, multi-page flows |

See `quality/quality-report.md` for the latest bloat critique and per-suite details.

**Test documentation:** See [`test-guidelines.md`](test-guidelines.md) for the authoritative pyramid rules, what belongs where, and bloat detection thresholds.

---

## E2E Test Strategy (FiremanDecko ↔ Loki)

### FiremanDecko (Implementation)

**Responsibility:** Write and maintain E2E tests that match critical user journeys.

- **Only test:** OAuth flows, Stripe integration, card CRUD, data persistence, multi-page navigation, a11y gates
- **Never test:** Static content, CSS measurements, regression suites, issue-scoped directories
- **After each feature:** Create/update E2E suite under `quality/test-suites/<feature>/`
- **Max 8 tests per suite** — if you hit 15, split by sub-feature or move to integration tests
- **Incremental commits** every 1-3 files changed: `wip: <what> — Ref #<issue>`
- **Verify:** `bash quality/scripts/verify.sh --step tsc` + `--step build` after each chunk

### Loki (Validation)

**Responsibility:** Critique and cull bloat. Validate that tests follow the pyramid.

- **Run bloat critique:** `bash quality/scripts/loki-critique.sh` after every coverage run
- **Review findings:** Check `quality/quality-report.md` for flagged files
- **Act on CRITICAL:** Files >15 tests = immediate consolidation issue filed
- **Act on WARNINGS:** 3+ warnings in one suite = file consolidation issue
- **Validate handoff:** Ensure all tests pass, tsc green, build green, before approving PR
- **Final QA:** PASS verdict includes new tests written and verifying acceptance criteria

---

## Current Test Coverage

**Latest snapshot:** `quality/quality-report.md` (auto-generated by `loki-critique.sh`)

Key metrics:
- **E2E suites:** 22 directories, 27 spec files
- **E2E tests:** ~159 (Playwright)
- **Unit/Integration:** ~802 tests (Vitest)
- **Overall health:** See `quality/quality-report.md` for real-time verdict

**Run tests:**
```bash
cd development/frontend

# Unit + Integration (fast)
npm run test:unit

# E2E only (requires server)
SERVER_URL=http://localhost:9653 npx playwright test ../../quality/test-suites/

# Full coverage (Vitest + Playwright merged)
cd ../.. && node quality/scripts/coverage.mjs
```

---

## Test Documentation

**Current:**
- [`test-guidelines.md`](test-guidelines.md) — Pyramid rules, bloat detection thresholds, migration rules
- [`quality-report.md`](quality-report.md) — Real-time bloat critique (auto-generated), wolf voice 🐺

**Sprint history:** Consult `git log --grep="Ref #" -- quality/` for per-story verdicts and test decisions. Do NOT maintain static sprint docs — git is the source of truth.

---

## QA Standards

**Every test must:**
1. Derive assertions from acceptance criteria, not from what the code currently does
2. Be idempotent — clear relevant localStorage before running
3. Map to a user journey or integration point, not to incidental implementation details
4. Avoid CSS measurements, static text assertions, and over-specification

**Every PASS verdict requires:**
- Code review clean
- Build clean (`npm run build`)
- TypeScript clean (`bash quality/scripts/verify.sh --step tsc`)
- GitHub Actions green
- New Playwright tests written and passing (Loki-approved)

---

## QA Critique Pipeline

Loki runs an automated bloat critique to keep the test suite lean and mean. See [`test-guidelines.md`](test-guidelines.md) §"Rules for E2E test suites" and §"Bloat Detection Rules" for the full checklist.

---

## Sprint History & Archival

For historical context on shipped features (Sprint 1–5), consult:
- `git log --grep="Story" --grep="Ref #" --grep="Fixes" -- .` for feature verdicts
- `git show <commit>` to review per-story QA decisions
- Per-issue tags in PR titles and branch names (see GitHub project board)

Sprint 1–5 are complete and shipped. New work is tracked via GitHub Issues → Project Board → Agent Chains.

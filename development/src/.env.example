# Fenrir Ledger — Environment Variables Template
# Copy this file to .env.local for local development
# Never commit .env or .env.local — they are gitignored

# App version (non-secret, safe for NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_APP_VERSION=1.0.0-sprint1

# Sprint 1: No secrets required. This file is a template for future sprints.
# Future sprints will add:
#   DATABASE_URL=...
#   NEXTAUTH_SECRET=...
#   NEXTAUTH_URL=...
